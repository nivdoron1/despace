// Export components
export { Login } from './components/Login';
export { Register } from './components/Register';

// Export hooks
export { useAuth } from './hooks/useAuth';

// Export types
export type { AuthConfig, AuthUser, AuthSession, LoginCredentials, RegisterCredentials, AuthState } from './types/auth';
